# URL del backend — el browser conecta directamente al backend
# Desarrollo local:
VITE_API_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000

# Producción (descomentar y ajustar):
# VITE_API_URL=https://api.tu-dominio.com/api
# VITE_SOCKET_URL=https://api.tu-dominio.com

# ── Puertos Docker (ajustar si hay conflicto con otros proyectos) ──
FRONTEND_PORT=8080
VITE_PORT=5173
